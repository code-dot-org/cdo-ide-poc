import React from "react";

export const DefaultEmptyEditor = () => {
  return (
    <div>No files are open. Choose a file from the browser to the left.</div>
  );
};

export const BlankEmptyEditor = () => {
  return <div></div>;
};
